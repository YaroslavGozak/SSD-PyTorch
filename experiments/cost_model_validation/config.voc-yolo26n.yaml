seed: 20260913
model:
  backend: ultralytics
  weights: trained_models/voc-yolo26n/best.pt
  device: cpu
  stride: 32
experiment_a:
  schedule_seed: 20260913
  prewarm_passes: 2
  primary_shape_statistic: trimmed_mean
  trim_fraction_each_tail: 0.10
  primary_fit_level: shape_level
  breakpoint_min_support: 2
  bootstrap_seed: 0
  max_invalid_bootstrap_fraction: 0.20
  fit_level_relative_difference_threshold: 0.20
  session_coefficient_cv_threshold: 0.20
  control_every_blocks: 2
  control_shapes: [[160, 160], [160, 320], [320, 320]]
  control_drift_threshold: 0.10
  timing_mode: inference_only
  max_requested_hw: [640, 640]
  area_fractions: [0.05, 0.10, 0.15, 0.20, 0.30, 0.40, 0.50, 0.60, 0.70, 0.80, 0.90, 1.0]
  global_warmup_iterations: 50
  repetitions_per_effective_shape: 40

experiment_b:
  canvas_hw: [640, 640]
  max_pair_generation_batches: 100
  domain_policy: reject_out_of_domain
  pair_count: 500
  repetitions: 20
  timing_mode: inference_only
  control_every_pairs: 25
  control_shapes: [[160, 160], [160, 320], [320, 320]]

  generator_seed: 20260913
  strata_quotas:
    broad_random: 0.20
    linear_boundary: 0.20
    piecewise_boundary: 0.20
    quadratic_boundary: 0.20
    model_disagreement: 0.20
  boundary_width_ms: 0.5
  max_generation_attempts: 5000000
  bootstrap_count: 2000
  bootstrap_seed: 0
  confidence_level: 0.95
  order_effect_warning_ms: 0.5
  control_drift_threshold: 0.10

decision_stability:
  stable_separate_max_probability: 0.10
  stable_merge_min_probability: 0.90
quality_gates:
  default: fail
  control_drift: warn
  invalid_bootstrap: warn
publication_run: false
